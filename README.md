# 🐯 RegisterSystem_Sci (ระบบลงทะเบียนงานคืนเหย้า "เสือเหลืองคืนถิ่น")

**RegisterSystem_Sci** คือเว็บแอปพลิเคชันสำหรับบริหารจัดการงานคืนเหย้า คณะวิทยาศาสตร์ จุฬาลงกรณ์มหาวิทยาลัย (Atoms In Ground Stage 109) ครอบคลุมการทำงานตั้งแต่การลงทะเบียนล่วงหน้า (Pre-registration) การลงทะเบียนหน้างาน (Kiosk) การรับบริจาค และการตรวจสอบสถิติสำหรับผู้ดูแลระบบ

---

## 📑 สารบัญ
1. [ฟีเจอร์หลัก (Key Features)](#-ฟีเจอร์หลัก-key-features)
2. [เทคโนโลยีที่ใช้ (Tech Stack)](#-เทคโนโลยีที่ใช้-tech-stack)
3. [ขั้นตอนการติดตั้ง (Installation)](#-ขั้นตอนการติดตั้ง-installation)
4. [การตั้งค่า Environment Variables](#-การตั้งค่า-environment-variables)
5. [โครงสร้างโปรเจกต์ (Project Structure)](#-โครงสร้างโปรเจกต์-project-structure)
6. [API Documentation](#-api-documentation)

---

## ✨ ฟีเจอร์หลัก (Key Features)

### 🖥️ ฝั่งผู้ใช้งาน (Frontend)
- **Pre-Registration:**
  - แบบฟอร์มลงทะเบียนที่จัดหมวดหมู่ข้อมูลชัดเจน (ข้อมูลส่วนตัว, การศึกษา, ติดต่อ, ที่อยู่)
  - รองรับการเลือกสถานะสมาชิก (สมาชิกเดิม / สมัครใหม่ / ไม่สมัคร)
  - ระบบแจ้งความประสงค์บริจาคเงิน (Donation) พร้อมแนบสลิป
- **Kiosk Mode:** โหมดสำหรับเจ้าหน้าที่ (Staff) ใช้ลงทะเบียนหน้างาน (Walk-in) อย่างรวดเร็ว
- **E-Ticket:** สร้างบัตรเข้างานพร้อม QR Code ทันทีที่ลงทะเบียนสำเร็จ (บันทึกเป็น PDF/PNG)
- **UI/UX:**
  - ดีไซน์สวยงาม ทันสมัย
  - มีสัญลักษณ์ **โบว์ไว้อาลัย (Mourning Ribbon)** ที่มุมจอ
  - ช่องกรอกปี พ.ศ. ออกแบบพิเศษให้อ่านง่ายสำหรับผู้สูงวัย
- **Security:** ป้องกัน Spam ด้วย Cloudflare Turnstile

### ⚙️ ฝั่งระบบหลังบ้าน (Backend & Admin)
- **Authentication:** ระบบยืนยันตัวตนแบบ JWT แยกสิทธิ์ Admin และ Staff
- **Dashboard:** กราฟสรุปยอดผู้ลงทะเบียน, ยอดเงินบริจาค, จำนวนผู้ติดตาม และยอด Check-in
- **Management:** ระบบจัดการรายชื่อ ค้นหา แก้ไข และลบข้อมูลผู้เข้าร่วม
- **Dynamic Fields:** สามารถตั้งค่าเปิด-ปิดฟิลด์ข้อมูลที่ต้องการให้กรอกได้
- **Check-in System:** รองรับการสแกน QR Code เพื่อเช็คชื่อเข้างาน

---

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)

### Frontend
- **Framework:** React (Vite)
- **UI Library:** Material UI (MUI) v5
- **HTTP Client:** Axios
- **Utilities:** QRCode.react, html2canvas, jspdf, dayjs

### Backend
- **Runtime:** Node.js
- **Framework:** Express.js
- **Database:** MongoDB (Mongoose)
- **Authentication:** JSON Web Token (JWT)
- **File Upload:** Multer
- **Email:** Nodemailer

---

## 🚀 ขั้นตอนการติดตั้ง (Installation)

### 1. สิ่งที่ต้องเตรียม (Prerequisites)
- [Node.js](https://nodejs.org/) (v16+)
- [MongoDB](https://www.mongodb.com/) (Local หรือ Atlas)

### 2. การติดตั้ง Backend
1. เข้าไปที่โฟลเดอร์ `backend` และติดตั้ง dependencies:

   ```bash
   cd backend
   npm install
   ```

2.  สร้างไฟล์ `.env`

3.  รัน Server:
    ```bash
    npm run dev
    # Server จะรันที่ http://localhost:5000
    ```

### 3\. การติดตั้ง Frontend

1.  เข้าไปที่โฟลเดอร์ `frontend` และติดตั้ง dependencies:
    ```bash
    cd frontend
    npm install
    ```
2.  สร้างไฟล์ `.env` 
3.  รัน Client:
    ```bash
    npm run dev
    # เว็บไซต์จะรันที่ http://localhost:5173
    ```

-----

## 🔧 การตั้งค่า Environment Variables

### Backend (`backend/.env`)

```env
PORT=5000
MONGO_URI=mongodb://localhost:27017/register_system_db
JWT_SECRET=your_super_secret_key_change_me
CORS_ORIGIN=http://localhost:5173,[https://your-production-domain.com](https://your-production-domain.com)

# Email Settings (สำหรับส่ง E-Ticket)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-password

# Cloudflare Turnstile (Backend Verification)
TURNSTILE_SECRET_KEY=your_turnstile_secret_key
```

### Frontend (`frontend/.env`)

```env
# URL ของ Backend API
VITE_API_BASE_URL=http://localhost:5000/api

# Cloudflare Turnstile (Frontend Widget)
VITE_TURNSTILE_SITE_KEY=your_turnstile_site_key
```

-----

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
RegisterSystem_Sci/
├── backend/                # API Server Logic
│   ├── src/
│   │   ├── config/         # Database Connection
│   │   ├── controllers/    # Request Handlers
│   │   ├── middleware/     # Auth, Logging, Validation
│   │   ├── models/         # Mongoose Schemas
│   │   ├── routes/         # API Routes Definition
│   │   └── utils/          # Helpers (Email, Upload, OTP)
│   └── app.js
│
└── frontend/               # React Client
    ├── public/             # Static Assets (Images)
    ├── src/
    │   ├── components/     # Reusable UI (Dialogs, Ribbon)
    │   ├── pages/          # Full Pages (PreRegister, Kiosk)
    │   ├── providers/      # Context (Auth)
    │   └── utils/          # API & Helpers
    └── vite.config.js
```
-----

## 📚 API Documentation

**Base URL:** `/api`  
**Authentication:** Bearer Token (`Authorization: Bearer <token>`)

### 🔐 1. Authentication

#### `POST /auth/login`

เข้าสู่ระบบสำหรับผู้ดูแลระบบ (Admin) หรือเจ้าหน้าที่ (Staff)

  - **Body:**
    ```json
    { "username": "admin", "password": "password123" }
    ```
  - **Response:** คืนค่า JWT Token และข้อมูล User

#### `GET /auth/me`

ดึงข้อมูลผู้ใช้ปัจจุบันจาก Token

  - **Header:** `Authorization: Bearer <token>`

-----

### 👥 2. Participants (ผู้เข้าร่วมงาน)

#### `POST /participants/create`

ลงทะเบียนผู้เข้าร่วมงาน (หน้า Pre-Register / Public)

  - **Body:**
    ```json
    {
      "name": "สมชาย ใจดี",
      "phone": "0812345678",
      "dept": "เคมี",
      "date_year": "2540",
      "consent": "agreed", // agreed หรือ disagreed
      "followers": 1,
      "cfToken": "turnstile_token",
      "usr_add": "123 ถ.พญาไท...",
      "usr_add_post": "10330"
    }
    ```

#### `GET /participants/list`

ดึงรายชื่อผู้ลงทะเบียนทั้งหมด (Admin Only)

  - **Header:** Require Admin Token
  - **Query:** `page=1`, `limit=20`, `search=keyword`

#### `GET /participants/:id`

ดูรายละเอียดผู้เข้าร่วมรายบุคคล

#### `PUT /participants/update/:id`

แก้ไขข้อมูลผู้เข้าร่วม (Admin Only)

#### `DELETE /participants/delete/:id`

ลบข้อมูลผู้เข้าร่วม (Admin Only)

-----

### 📍 3. Registration Points (Kiosk)

#### `POST /registration-points/register-walkin`

ลงทะเบียนหน้างานโดยเจ้าหน้าที่ (Kiosk Mode)

  - **Header:** Require Staff Token
  - **Body:**
    ```json
    {
      "name": "สมหญิง รักเรียน",
      "phone": "0899999999",
      "dept": "ฟิสิกส์",
      "date_year": "2545",
      "registrationPoint": "จุดลงทะเบียนตึกแถบ",
      "consent": "agreed"
    }
    ```

-----

### 💰 4. Donations (การบริจาค)

#### `POST /donations/create`

บันทึกข้อมูลการแจ้งโอนเงิน

  - **Body:**
    ```json
    {
      "userId": "participant_id (optional)",
      "firstName": "สมชาย",
      "lastName": "ใจดี",
      "amount": 500,
      "transferDateTime": "2025-02-21T10:30:00.000Z",
      "source": "PRE_REGISTER"
    }
    ```

#### `GET /donations/summary`

ดูสรุปยอดเงินบริจาคทั้งหมด (Admin Only)

-----

### 📊 5. Dashboard (สถิติ)

#### `GET /dashboard/summary`

ดึงข้อมูลสถิติภาพรวมเพื่อแสดงกราฟ

  - **Header:** Require Admin Token
  - **Response Example:**
    ```json
    {
      "totalParticipants": 200,
      "totalFollowers": 50,
      "checkinCount": 120,
      "byDept": [{ "_id": "เคมี", "count": 50 }, ...],
      "byYear": [{ "_id": "2540", "count": 20 }, ...]
    }
    ```

-----

### ⚙️ 6. System Configuration

#### `GET /participant-fields`

ดึงรายการฟิลด์ข้อมูลที่เปิดใช้งาน (ใช้สำหรับสร้าง Dynamic Form หน้าบ้าน)

#### `POST /participant-fields/update`

ตั้งค่าเปิด/ปิด หรือเปลี่ยนลำดับฟิลด์ (Admin Only)

-----

### 📩 7. Utilities

#### `POST /utils/send-ticket`

ส่งอีเมล E-Ticket ให้ผู้เข้าร่วมซ้ำ (Manual Resend)

  - **Body:** `{ "participantId": "..." }`

-----

## 📝 Status Codes Reference

| Code | Description |
| :--- | :--- |
| **200** | OK - ทำรายการสำเร็จ |
| **201** | Created - สร้างข้อมูลใหม่สำเร็จ |
| **400** | Bad Request - ข้อมูลที่ส่งมาไม่ถูกต้อง |
| **401** | Unauthorized - ไม่ได้ล็อกอิน หรือ Token หมดอายุ |
| **403** | Forbidden - ไม่มีสิทธิ์เข้าถึง (Access Denied) |
| **404** | Not Found - ไม่พบข้อมูล/API ที่เรียก |
| **500** | Internal Server Error - ระบบขัดข้อง |



